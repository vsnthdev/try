/*
 *  Entry Next.js page for try project.
 *  Created On 23 October 2022
 */

import '../styles/index.css'
import { AppProps } from 'next/app'

export default ({ Component, pageProps }: AppProps) => <Component { ...pageProps } />
